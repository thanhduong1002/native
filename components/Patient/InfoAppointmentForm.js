import {
    View,
    Text,
    Keyboard,
    StyleSheet,
    SafeAreaView,
    ImageBackground,
    Image,
    TextInput,
    TouchableOpacity,
    KeyboardAvoidingView,
    TouchableWithoutFeedback,
    Platform,
    ScrollView,
    Alert,
} from 'react-native';

import React, { useState } from 'react';

import { Ionicons } from '@expo/vector-icons';
const IMAGE_BACKGROUND = require('../../assets/image/image_title_login.jpeg');
const PatientConfirmedForm = ({ navigation }) => {
    return (
        <ScrollView style={{ backgroundColor: 'white' }}>
            <View><Text style={{fontSize:30}}>oke</Text></View>
        </ScrollView>
    );
};

const styles = StyleSheet.create({
    
});

export default PatientConfirmedForm;
